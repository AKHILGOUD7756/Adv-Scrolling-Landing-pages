<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Sticky Cards Scroll Animation</title>

<!-- Fonts -->
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500&family=Roboto+Mono:wght@400&display=swap" rel="stylesheet">

<style>
/* ---------------- RESET ---------------- */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body {
  height: 100%;
}

body {
  font-family: "Inter", system-ui, sans-serif;
  background: #111;
  color: #fff;
}

/* ---------------- GLOBAL ---------------- */
img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

section {
  position: relative;
  width: 100vw;
  height: 100vh;
  overflow: hidden;
}

/* ---------------- HERO / OUTRO ---------------- */
.hero,
.outro {
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 1rem;
  background: #202020;
}

.hero h1,
.outro h1 {
  font-size: 3rem;
  font-weight: 400;
  line-height: 1.5;
  max-width: 80vw;
  text-transform: uppercase;
}

/* ---------------- STICKY CARDS ---------------- */
.sticky-cards {
  position: relative;
  background: #e3e3e3;
}

/* Card */
.card {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 100%;
  max-width: 450px;
  height: 55%;
  padding: 1rem;
  background: #fff;
  color: #202020;
  border-radius: 6px;
  transform: translate(-50%, -50%);
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  will-change: transform;
}

.card-img {
  flex: 1 1 auto;
  border-radius: 4px;
  overflow: hidden;
}

.card-content {
  padding: 0.5rem;
}

.card-content h5,
.card-content p {
  text-transform: uppercase;
  letter-spacing: 0.06em;
}

.card-content h5 {
  font-family: "Roboto Mono", monospace;
  font-size: 18px;
  margin-bottom: 0.25rem;
}

.card-content p {
  font-size: 12px;
}

/* ---------------- RESPONSIVE ---------------- */
@media (max-width: 900px) {
  .hero h1,
  .outro h1 {
    font-size: 2rem;
  }

  .card {
    width: 80%;
    height: 60%;
  }
}
</style>
</head>

<body>

<!-- HERO -->
<section class="hero">
  <h1>
    Every new interface is a chance to rebuild the future one line,
    one idea, one experience.
  </h1>
</section>

<!-- STICKY CARDS -->
<section class="sticky-cards">

  <div class="card">
    <div class="card-img">
      <img src="https://images.pexels.com/photos/787961/pexels-photo-787961.jpeg">
    </div>
    <div class="card-content">
      <h5>Fun Corporate Events</h5>
      <p>Lorem ipsum dolor sit amet consectetur.</p>
    </div>
  </div>

  <div class="card">
    <div class="card-img">
      <img src="https://images.pexels.com/photos/1566837/pexels-photo-1566837.jpeg">
    </div>
    <div class="card-content">
      <h5>Pizza & Toppings</h5>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </div>
  </div>

  <div class="card">
    <div class="card-img">
      <img src="https://images.pexels.com/photos/2174656/pexels-photo-2174656.jpeg">
    </div>
    <div class="card-content">
      <h5>Travel & Tickets</h5>
      <p>Lorem ipsum dolor sit amet consectetur.</p>
    </div>
  </div>

  <div class="card">
    <div class="card-img">
      <img src="https://images.pexels.com/photos/1189257/pexels-photo-1189257.jpeg">
    </div>
    <div class="card-content">
      <h5>Cocktails & Creativity</h5>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </div>
  </div>

  <div class="card">
    <div class="card-img">
      <img src="https://images.pexels.com/photos/1710053/pexels-photo-1710053.jpeg">
    </div>
    <div class="card-content">
      <h5>Awards & Glory</h5>
      <p>Lorem ipsum dolor sit amet consectetur.</p>
    </div>
  </div>

</section>

<!-- OUTRO -->
<section class="outro">
  <h1>
    The world will always evolve.
    Your code should evolve faster.
  </h1>
</section>

<!-- GSAP + ScrollTrigger -->
<script src="https://cdn.jsdelivr.net/npm/gsap@3/dist/gsap.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/gsap@3/dist/ScrollTrigger.min.js"></script>

<!-- LENIS -->
<script src="https://unpkg.com/lenis@1.3.15/dist/lenis.min.js"></script>

<script>
document.addEventListener("DOMContentLoaded", () => {

  gsap.registerPlugin(ScrollTrigger);

  /* -------- LENIS -------- */
  const lenis = new Lenis({ smooth: true });
  lenis.on("scroll", ScrollTrigger.update);
  gsap.ticker.add(t => lenis.raf(t * 1000));
  gsap.ticker.lagSmoothing(0);

  /* -------- CARDS -------- */
  const cards = gsap.utils.toArray(".card");
  const rotations = [-12, 10, -6, 6, -4];

  cards.forEach((card, i) => {
    gsap.set(card, {
      y: window.innerHeight,
      rotate: rotations[i % rotations.length]
    });
  });

  /* -------- SCROLLTRIGGER -------- */
  ScrollTrigger.create({
    trigger: ".sticky-cards",
    start: "top top",
    end: "+=400%",
    pin: true,
    scrub: 1,

    onUpdate(self) {
      const progress = self.progress;
      const slice = 1 / cards.length;

      cards.forEach((card, i) => {
        const start = i * slice;
        let p = (progress - start) / slice;
        p = gsap.utils.clamp(0, 1, p);

        let y = window.innerHeight * (1 - p);
        let x = 0;

        if (p === 1 && i < cards.length - 1) {
          const drift = (progress - (start + slice)) * 2;
          if (drift > 0) {
            x = -window.innerWidth * 0.25 * drift;
            y = -window.innerHeight * 0.25 * drift;
          }
        }

        gsap.set(card, { x, y });
      });
    }
  });

});
</script>

</body>
</html>
